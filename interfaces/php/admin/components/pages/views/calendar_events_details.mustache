<h2>Event details</h2>
<p>
	Quickly add a date to the system. Information about published/unpublished status and
	cancellation can be found in the page tips.
</p>

<p><span class="highlightcopy">
	The date and venue selectors are far from ideal. We'll be enhancing these with fancy
	interfaces soon.
</span></p>

<form method="post" action="{{www_path}}{{requested_route}}">
	<input type="hidden" name="{{form_state_action}}" value="makeitso" />

	<label for="event_date">Date</label><br />
	<input type="text" id="event_date" name="event_date" value="{{formatted_date}}"{{^formatted_date}} placeholder="MM/DD/YYYY"{{/formatted_date}} />

	<div class="row_seperator">.</div>

	<label for="event_venue">Venue</label><br />
	<select id="event_venue" name="event_venue">
		<option value="0" selected="selected">Unknown (Will be listed as 'TBA')</option>
		{{{venue_options}}}
	</select>

	<div class="row_seperator">.</div>
	<label for="event_comment">Comments (Support, CD Release, etc)</label><br />
	<textarea rows="3" id="event_comment" name="event_comment">{{comments}}</textarea>

	<div class="row_seperator">.</div>
	<label for="event_purchase_url">Ticket Purchase URL</label><br />
	<input type="text" id="event_purchase_url" name="event_purchase_url" value="{{purchase_url}}"{{^purchase_url}} placeholder="Ticket Link URL"{{/purchase_url}} />

	<div class="row_seperator">.</div>
	<input type='checkbox' class='checkorradio' name='event_ispublished' value=''{{#published}} checked="checked"{{/published}} /> Published?
	&nbsp; <input type='checkbox' class='checkorradio' name='event_iscancelled' value=''{{#cancelled}} checked="checked"{{/cancelled}} /> Cancelled?

	<div class="row_seperator">.</div><br />
	<input class="button" type="submit" value="{{event_button_text}}" />

</form>